{
  "name": "jsonserver-backend",
  "version": "1.0.0",
  "description": "a JSON Server Backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch database.json"
  },
  "author": "Hans Cristian",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
